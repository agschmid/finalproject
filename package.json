{  "scripts": {
    "predeploy": "npx vite build --base=finalproject",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "gh-pages": "^6.0.0",
    "three": "^0.157.0"
  },
  "devDependencies": {
    "vite": "^4.5.0",
    "vite-plugin-mkcert": "^1.16.0"
  }
}
